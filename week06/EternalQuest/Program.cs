using System;

class Program
{
    static void Main(string[] args)
    {
        // Instantiate the GoalManager and start the program loop
        GoalManager manager = new GoalManager();
        manager.Start();
    }
}